*** Install json-server globally in node.js (1 time thing)

npm install -g json-server

*** Startup json-server with feed from 'posts.json' (see 'posts.json' content).
** run the following command in the console where 'posts.json' is:

json-server posts.json

